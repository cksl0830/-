def solution(arr1, arr2):

    for i in range(len(arr1)):
        for j in range(len(arr1[i])):
            arr1[i][j]+=arr2[i][j]

    return arr1
    
    
 // 다른 사람의 풀이 # 감탄한 풀이 .. zip(*x) 를 하면 행렬의 같은 위치끼리 묶어준다 
 
 def sumMatrix(A,B):
    return [list(map(sum, zip(*x))) for x in zip(A, B)]
